replicaCount: 1
image:
  repository: ghcr.io/fitnest-backend/catalog-service
  tag: "latest"
  pullPolicy: IfNotPresent
  imagePullSecrets:
    - name: ghcr-secret
service:
  type: ClusterIP
  port: 8080
istio:
  enabled: true
  host: "catalog-service"
env:
  SPRING_DATASOURCE_URL: "jdbc:postgresql://postgresql-catalog:5432/fitnest_catalog"
  SPRING_DATASOURCE_USERNAME: "fitnest_catalog"
  SPRING_DATASOURCE_PASSWORD: "catalog_password_123"
  SPRING_JPA_HIBERNATE_DDL_AUTO: "update"
resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
